#include <bits/stdc++.h>
using namespace std;

int N[100], testCase[10000], testIndex = 0, T;

int main(){
    cin >> T;
    for(int i = 0; i < T; i++){
        cin >> N[i];
        for(int j = 0; j < N[i]; j++) cin >> testCase[testIndex + j];
        testIndex += N[i];}
    
    testIndex += 0;
    for(int i = 0; i < T; i++){
        for(int j = 0; j < N[i]; j++){
            
        }
    }
}